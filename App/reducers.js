import { combineReducers } from 'redux';

import { ADD_QUOTE, DELETE_QUOTE, APDATE_QUOTE, GET_QUOTES } from './actions'

const dataState = { quotes: [] }; 

const dataRducer = (dataState, action) =>
{
    switch 
}